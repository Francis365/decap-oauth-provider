# Copy these into your hosting platform environment variables
# (Do not commit a real .env with secrets)

# GitHub OAuth App credentials
OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=

# If you prefer alternative names, the code also checks:
# GITHUB_CLIENT_ID / OAUTH_GITHUB_CLIENT_ID
# GITHUB_CLIENT_SECRET / OAUTH_GITHUB_CLIENT_SECRET

# Provider public callback
# Example: https://femme-essentials-oauth.onrender.com/callback
REDIRECT_URL=

# Comma-separated hostnames allowed to open /admin and authenticate
# e.g. femme-essentials.onrender.com, francis365.github.io
ORIGINS=

# Scopes for GitHub access token
# public_repo (public repos) or repo (private repos)
SCOPES=public_repo

